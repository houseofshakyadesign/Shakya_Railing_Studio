# Frontend
VITE_API_URL="http://localhost:5000/api"

# Server (copy to server/.env)
JWT_SECRET="replace-with-a-strong-random-secret"
CORS_ORIGINS="http://localhost:5173,http://localhost:3000,https://shakya-railing-studio.vercel.app"
PORT=5000

# MySQL (optional - falls back to SQLite if unreachable)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=metalwork_nepal
